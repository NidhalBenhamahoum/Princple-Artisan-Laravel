@extends('layouts.app')

@section('content')
<a href="voir_panier" class="btn btn-primary">Voir Panier</a>
<a href="voir_produits" class="btn btn-danger">Voir Produits</a>
<div class="container">
    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="card">
                <div class="card-header">{{ __('Dashboard') }}</div>

                <div class="card-body">
                    @if (session('status'))
                        <div class="alert alert-success" role="alert">
                            {{ session('status') }}
                        </div>
                    @endif


                </div>
            </div>
        </div>
    </div>
</div>
@endsection
